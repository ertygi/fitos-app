{
  "name": "fitos-app-monorepo",
  "version": "1.0.0",
  "description": "Root package for FitOS application",
  "scripts": {
    "start": "npm start --prefix backend",
    "heroku-postbuild": "npm install --prefix backend && npm install --prefix frontend --legacy-peer-deps && npm run build --prefix frontend"
  },
  "engines": {
    "node": "18.x"
  },
  "author": "",
  "license": "ISC"
}
